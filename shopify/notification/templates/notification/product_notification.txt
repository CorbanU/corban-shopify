{{ billing_address.first_name }} {{ billing_address.last_name }} placed order {{ name }} at {{ created_at|date:"SHORT_DATETIME_FORMAT" }}

{{ product.quantity }}x {{ product.name }} for ${{ product.price }} each
{% for property in product.properties %}
  {{ property.name }}: {{ property.value }}
{% endfor %}
{{ note }}
